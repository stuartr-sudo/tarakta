# Tarakta MM — Status, 2026-07-18

**Branch:** `main` = `codex/tarakta-stabilization` = `9500ad2` (fast-forwarded + pushed)
**Tests:** 776 passing, 1 skipped
**Local bot:** STOPPED pending `launchctl load` (see "User actions pending")
**Fly prod:** still running the stale April 28 build — decision pending (see §4)

Previous: [`STATUS_2026-04-28.md`](./STATUS_2026-04-28.md)

---

## Headline

Full profitability review completed and the committee's LLM transport rebuilt to run on the Claude Code subscription (no `ANTHROPIC_API_KEY` needed). The committee had been silently dead since April — every call since 2026-06-15 logged `ERROR client_unavailable` (216 rows) while shadow mode converted the errors to APPROVE.

## 1. Why the bot isn't profitable (evidence from Supabase, May 1 → Jul 16, instance `tarakta-mm`)

| Bleed | N | P&L | Mechanism |
|---|---:|---:|---|
| `stop_loss` | 41 | −$10,995 | avg −1.39R (fills at 5-min-poll price, overshooting tight SLs); 36/41 never armed breakeven despite avg MFE 0.46R (BE waits for Level-2 advance that rarely fires); 27/36 never reached +0.5R (entry quality — the dead selection layer's job) |
| `scratch_2h` | 35 | −$2,944 | closes at market at the 2h mark wherever price is; 7 closes worse than −0.3R cost −$2,363 |
| Winners | 9 | +$4,441 | MFE 6–8R realized as 0.5–1.7R |

The deterministic engine alone has negative expectancy (also documented in `CODEX_PLAN.md`); the single-agent Opus veto era (Apr 20–27, 97% veto) was the only profitable configuration. The inverse mirror also lost (−$1,092/27) → candidate stream ~random + costs, not invertible. Recommend retiring it.

## 2. What shipped today (`9500ad2`)

`src/strategy/mm_claude_cli.py` — `ClaudeCLIClient`: committee model calls via `claude -p --output-format json --tools "" --strict-mcp-config --no-session-persistence`, auto-selected by `MMCommittee._get_client()` when `anthropic_api_key` is empty. Child env scrubbed (observed: harness `SSL_CERT_FILE` breaks TLS; nested-session markers corrupt auth). `claude_code_oauth_token` config → child `CLAUDE_CODE_OAUTH_TOKEN` (pydantic `.env` values never reach `os.environ`). Cost short-circuit for subscription runs. CLI-aware committee deadline. 12 tests. CHANGELOG entry with full detail.

Live E2E verified the whole chain (backend selection → 5 specialists spawned → error classified → `mm_agent_decisions` row with committee JSONB → shadow APPROVE). Currently 401s because the machine's CLI login is stale (desktop app manages its own auth; Keychain CLI credentials rejected) — resolved by the setup-token below.

## 3. User actions pending (both ~30s)

1. `claude setup-token` in Terminal → add to `/Users/stuarta/tarakta/.env`: `CLAUDE_CODE_OAUTH_TOKEN=<token>` — activates real committee verdicts (shadow mode, vetoes nothing).
2. `launchctl load ~/Library/LaunchAgents/com.tarakta.mm.plist` — starts the bot under launchd (auto-start on boot, auto-restart on crash, logs to `~/tarakta-mm.log`). Plist already written. Stop with `launchctl unload …`.

## 4. Next steps (queued, in order)

1. **Stop Fly prod** (`tarakta-mm` app) — it runs the April build, ignored the June 3 `scanning_active:false` (settings only read at startup; machine never restarted), and has lost −$9.4k. Dashboard Stop button (in-process, works) or `fly auth login` + scale to 0 — or redeploy it from today's main.
2. **Reconcile orphaned NEAR short** on prod (id `ad27655d-…`, opened 7/1 06:04, untouched 17 days — restart double-entry; its twin took −$1,075).
3. **ERROR-streak alert** on `mm_agent_decisions` (216 consecutive ERRORs went unnoticed for 6 weeks).
4. **Engine fixes** (course-cited, replay-validated, per `CODEX_PLAN.md` Phase 0): stop-fill realism (fill at SL±slippage, not poll price — removes ~$3.1k/quarter of overshoot), level-advance/target audit (why 2–5R excursions never register L1 → BE ladder idle), scratch bound (a trade at −0.5R should meet its SL, not capitulate at 2h).
5. **Committee shadow → veto** after ≥2 weeks & ≥30 real assessments (design doc §7 gates), compared against outcomes.
6. Later, for Fly hosting of the committee: same `CLAUDE_CODE_OAUTH_TOKEN` as a Fly secret + `claude` CLI in the image (or keep LLM local-only).

## 5. Known accounting caveats

- `trades.pnl_usd` is cumulative (includes partials) — never add `partial_exits.pnl_usd` on top.
- `fees_usd` inconsistent on prod (~$0 recorded; paper default taker 0.26% is Kraken-ish vs Binance futures 0.05%) — normalize before trusting fine-grained P&L.
- `main`-instance rows have tiny/garbage `risk_usd` on some stops (avg −15R artifacts) — R-stats on that instance unreliable pre-`15bf19e`.
